{
  "name": "lifelog-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Lifelog cloud sync backend: accounts + last-write-wins snapshot sync.",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "cors": "^2.8.5",
    "express": "^4.21.1",
    "express-rate-limit": "^7.4.0",
    "helmet": "^8.0.0",
    "nodemailer": "^9.0.3"
  }
}
